services:
  backend:
    image: tng1/daab-server:master
    pull_policy: always
    ports:
      - 5035:8080
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    env_file:
      - .env.local
